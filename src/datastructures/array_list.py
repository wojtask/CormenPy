class MultipleArrayList:
    def __init__(self, key, next, prev, head, free):
        self.key = key
        self.next = next
        self.prev = prev
        self.head = head
        self.free = free
